# Optional additional trusted origins for a separate voice proxy.
WATCHTOWER_ORIGINS=
# No QCA or iFinD credential belongs in deployment environment variables.
# Each user supplies their own credential in the browser.
